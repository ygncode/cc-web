{
  "name": "cc-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,web -c blue,green \"CC_WEB_CWD=$PWD bun run --filter=@cc-web/server dev\" \"bun run --filter=@cc-web/web dev\"",
    "build": "bun run --filter=* build",
    "start": "CC_WEB_CWD=$PWD bun run --filter=@cc-web/cli start"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "typescript": "^5.7.2"
  }
}
